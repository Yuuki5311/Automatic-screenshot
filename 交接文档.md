# 王者荣耀云游戏自动截图 — 交接文档

面向接手本仓库的同学：先能跑起来、再能改任务、出了问题知道去哪查。更细的运行时状态机、SDD 流程见 `docs/PROJECT_WORKFLOW.md`。

---

## 1. 这是什么

Windows 桌面工具。通过 Selenium 打开腾讯先锋（`gamer.qq.com`），启动王者荣耀云游戏，用 OpenCV 模板匹配在游戏 UI 里点路径，按固定任务列表截图保存。

| 项 | 内容 |
|----|------|
| 语言 / GUI | Python 3.12 + Tkinter |
| 浏览器 | Windows：系统 Edge；macOS：Chrome（开发机以 Windows 为准） |
| 点击方式 | CDP `Input.dispatchMouseEvent`，不用系统鼠标、不用 Selenium `element.click()` |
| 匹配 | `cv2.matchTemplate`，默认阈值 `0.53`（云游戏画面有压缩） |
| 打包 | PyInstaller onefile + GitHub Actions Nightly |

**视口锁定为 1920×1010 CSS 像素。** 换分辨率必须重采模板和坐标，否则匹配/点击会偏。

---

## 2. 环境与启动

### 2.1 开发运行

```powershell
cd C:\Automatic-screenshot
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
python main.py
```

需要：Python 3.12、本机 Microsoft Edge。首次启动会由 Selenium Manager 拉 EdgeDriver。

### 2.2 给运营同学的 EXE

- CI：推 `main` 或手动跑 `.github/workflows/build.yml`
- 产物：`dist/AutoScreenshot.exe`，同时挂到 GitHub Release tag `nightly`（prerelease）
- 本地打包：`pyinstaller AutoScreenshot.spec`（与 workflow 等价：`--onefile --windowed`）
- EXE 内嵌 `templates/` 和 `calibrated_coords.json`；**截图和日志写在 EXE 同目录**，不是临时解压目录

### 2.3 输出位置

| 内容 | 开发环境 | 打包 EXE |
|------|----------|----------|
| 截图 | `screenshots/<账号名>/` | `<exe所在目录>/screenshots/<账号名>/` |
| 日志 | `logs/YYYY-MM-DD_HH-MM-SS.log` | 同上，相对 EXE 目录 |

账号框为空时文件夹名为 `unknown_HHMMSS`。

---

## 3. 操作员怎么用

1. 打开程序，选 **腾讯先锋登录**：QQ 扫码 / 微信扫码 / QQ 密码（手动）。
2. 选 **游戏内登录平台**：微信/QQ × iOS/安卓。
3. 填 **账号**（只当截图文件夹名，不登录游戏账号）。
4. 点「启动」。
   - 扫码：窗口内出二维码，用手机扫。
   - 密码：浏览器打开先锋站后，人在浏览器里登完，再点 GUI 的「完成登录 →」。
5. 后台会：登先锋 → 搜王者点秒玩 → 切云游戏标签 → 尽量先退出已登录角色 → 点平台进游戏 → 授权按钮 → 进大厅 → 按任务截图。
6. 本轮结束后会关云游戏标签、清 Storage，回到待命页，可再开一轮。关窗口会清 EdgeDriver 残留。

扫码超时约 300s；手动登录轮询约 600s。

---

## 4. 运行时四阶段（改代码时对照）

入口：`gui/app.py` 的 `_run_workflow()`（后台 daemon 线程）。GUI 只通过 `queue.Queue` 收消息，**不要在 worker 里碰 Tk 控件**。

| 阶段 | 做什么 | 主要文件 |
|------|--------|----------|
| 1 先锋登录 | `web_login()` 或 `manual_login()` | `login.py`, `browser.py` |
| 2 启动游戏 | 搜索王者 → 秒玩 → 切 tab → 预退出感知环 | `game_launcher.py`, `ui_loop.py` (`run_pre_logout_loop`) |
| 3 游戏内登录 | 平台按钮 → `game_auth_login_1/2` → `enter_game`，最多 3 次 | `login.py` (`game_login`) |
| 4 感知环截图 | 分类 UI → 关弹窗优先 → 点路径 → 截图 → 返回 | `ui_loop.py`, `ui_state.py`, `screenshot_click.py` |

结束后关云游戏 tab，清 `localStorage`/`sessionStorage`，避免云容器重连脏状态。

**截图任务列表写死在** `gui/app.py` 约 741 行 `screenshot_tasks`。每项是 `(名称, 点击序列, 返回次数)`。点完且截完后，按 `back_count` 点 `back_arrow.png`。

当前约 19 个任务：主页、英雄、万象图鉴首页/灵宝、按键、天幕、星典藏/星传说、皮肤图鉴及若干子页、积分夺宝、货币背包、小兵、个性戳戳、贵族。

点击项特殊标记：

- `"__coords__"`：用 `calibrated_coords.json` 的像素点（目前主路径里常用 `avatar`、`minion`）
- `"__guard__"`：守卫（例如图鉴路径上的返回箭头）

---

## 5. 文件地图

日常改功能看这些即可：

| 文件 | 职责 |
|------|------|
| `main.py` | 入口：预加载 cv2/selenium（EXE 后台线程首次 import 会卡死）、孤儿进程清扫 |
| `gui/app.py` | 四页 UI、任务列表、workflow 编排 |
| `gui/widgets/` | 二维码、日志进度 |
| `config.py` | URL、视口、匹配阈值、路径工具 `resource_path` / `writable_path` |
| `browser.py` | 反检测、锁视口、创建 Edge/Chrome |
| `login.py` | 先锋登录 + 游戏内平台/授权登录 |
| `game_launcher.py` | 秒玩、切云游戏 tab |
| `navigator.py` | CDP 截视口、模板匹配、CDP 点击、坐标兜底 |
| `ui_state.py` | 一帧画面分类：弹窗 / 确认 / 登录 / 路径 / 主界面 / 未知 |
| `ui_loop.py` | FSM 感知环、预退出环、Goal 游标 |
| `screenshot_click.py` | 点前弹窗守卫、点击生效校验 |
| `click_confirm.py` | 两遍点击：定位计划 + ROI 再确认 |
| `popup_monitor.py` | 弹窗关闭 |
| `screenshotter.py` | 保存整页 PNG |
| `keybind_config.py` | 进游戏后改自定义键位（模板 + `keybind_pos` 坐标） |
| `process_cleanup.py` | Job Object + 退出杀 `msedgedriver` |
| `calibrated_coords.json` | CSS 坐标兜底（模板失败时） |
| `templates/` | 匹配用小图，缺图任务必挂 |

工具脚本（不当 GUI 入口）：

| 脚本 | 用途 |
|------|------|
| `capture_templates.py` | 交互框选重采模板 |
| `calibrate_coords.py` | 重采 `calibrated_coords.json` |
| `capture_one_coord.py` | 采单个坐标 |
| `client_launcher.py` | 客户端启动相关 |
| `probe_dom.py` | DOM 探测 |

测试（不依赖真游戏画面）：

```powershell
python -m pytest test_core.py test_optional_popup.py test_process_cleanup.py test_keybind_config.py -q
```

改感知环/点击解析后优先跑 `test_core.py` 和 `test_optional_popup.py`。

---

## 6. 改任务、改模板、改坐标

### 6.1 加/改一个截图页

1. 在 `templates/` 放对应 PNG（命名与代码字符串一致）。
2. 在 `gui/app.py` 的 `screenshot_tasks` 加一项或改点击链、`back_count`。
3. 若该按钮匹配不稳，用 `calibrate_coords.py` 写入同名 key，`navigator` 会在模板失败时点坐标。
4. 跑单元测试，再真机走一轮该页。

### 6.2 重采模板

```powershell
python capture_templates.py
python capture_templates.py --only tianmu.png xingyuan.png
```

流程：脚本开浏览器 → 人登录并进到目标页 → 切到 URL 含 `/v2/game/` 的 tab → 框选保存。

游戏改版后优先重采：`avatar.png`、`back_arrow.png`、弹窗关闭图、平台登录四张、`enter_game.png`、`game_auth_login_1/2.png`。

模板宜带一点周围 UI，太小容易误匹配。中文路径用 `np.fromfile` + `imdecode`，不要用 `cv2.imread`。

### 6.3 重采坐标

视口必须仍是 1920×1010。`calibrated_coords.json` 里部分 key 数值明显成组重复（例如皮肤子页、背包），若线上点偏，优先当「未单独校准」处理，不要盲信。

当前主路径仍会读的坐标：`avatar`、`minion`（以及键位模块的 `keybind_pos`）。

---

## 7. 必须遵守的约束

1. **Worker 不碰 Tk**；跨线程只用 Queue / `threading.Event`。
2. **点击走 CDP**，不要改回系统光标或普通 Selenium click（焦点会跳、易被检测）。
3. **不要在未评估影响时改 `web_login()` 内部**；历史需求是扫码与密码登录隔离（密码走 `manual_login()`）。
4. **弹窗优先于主线点击**；点前守卫失败应取消本轮主线点击，不要硬点。
5. **打包后重量级库必须在主线程预加载**（见 `main.py` `_preload_runtime`）。
6. **资源只读走 `resource_path`，可写走 `writable_path`**，否则 EXE 下截图会写进临时目录或写失败。
7. 提交由人确认后再做；不要把 `screenshots/`、`logs/`、`build/`、密钥写进 git。

---

## 8. 已知坑与排障

| 现象 | 常见原因 | 怎么查 |
|------|----------|--------|
| 点启动后浏览器不开 | EXE 下后台线程首次 import cv2/selenium；或 EdgeDriver 被杀软拦 | 看 `logs/`；确认走了预加载 |
| 截到先锋首页而不是游戏 | 没切到云游戏第二个 tab | `game_launcher.switch_to_new_tab`、日志里的 URL |
| 一直 UNKNOWN / 超时恢复 | 模板过期、阈值、弹窗挡画面 | 对照 `ui_state` 优先级；重采模板 |
| 点了没进下一页 | 生效校验失败或 ROI 未确认 | `screenshot_click` / `click_confirm` 日志 |
| 关软件后残留 Edge | 清理未跑完 | `process_cleanup`；任务管理器杀 `msedgedriver.exe` |
| 匹配全偏 | 窗口被最大化或 DPI 缩放破坏视口锁 | 确认 `lock_viewport`，勿手改窗口大小 |
| 登录检测不到 | iframe/Cookie 变了 | `login.py` 四种检测：弹窗消失、Cookie、JS、CSS |

反检测在 `browser.py`（隐藏 `navigator.webdriver`、伪造 plugins 等）+ `--disable-blink-features=AutomationControlled`。先锋站策略会变，登录突然全失败时优先怀疑检测升级，而不是任务列表。

云游戏 tab 很重：预退出环刻意 2s 一帧，截图像质 JPEG 70，避免 tab crash。

---

## 9. 文档与历史从哪读

| 路径 | 内容 |
|------|------|
| `docs/PROJECT_WORKFLOW.md` | SDD 流程、FSM 决策表、线程模型、反检测细节 |
| `docs/superpowers/specs/` | 各功能设计（登录、感知环、EXE 路径、弹窗等） |
| `docs/superpowers/plans/` | 对应实现计划 |
| `templates/README.md` | 模板文件名与含义（可能略旧于代码） |
| `.github/workflows/build.yml` | CI 打包命令的真相来源 |

`docs/PROJECT_WORKFLOW.md` 里「完成页」描述已过时：当前完成后回到待命页，用「再执行一轮」。截图张数以 `screenshot_tasks` 长度为准，不要死记「18 张」。

---

## 10. 接手后建议先做的事

1. 虚拟环境安装依赖，跑一遍 pytest。
2. 真机走通：QQ 扫码 + 一个游戏平台 + 账号名，确认 `screenshots/<账号>/` 有图。
3. 通读 `gui/app.py` 的 `_run_workflow` 和 `screenshot_tasks`。
4. 需要改版 UI 时，先 `capture_templates.py` 再改任务，不要先改阈值碰运气。
5. 仓库根目录有一个异常文件 `=4.0`（像 pip 重定向误生成），确认无用后可删，勿当依赖。

更细的模块行为以源码和 `docs/PROJECT_WORKFLOW.md` 为准；本文以「能交接、能排障」为目的，不重复全部状态机表格。
